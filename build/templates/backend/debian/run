{{define "main" -}}
#!/bin/bash
set -e
if [[ "$(whoami)" != root ]]; then
 SUPERUSER=sudo
fi
$SUPERUSER bash "HOME={{.Env.BackendDataPath}}/{{.Coin.Alias}}/backend {{template "Backend.ExecCommandTemplate" .}}"
{{end}}
